export class UserCreatedEvent {
    constructor(
        public readonly userId: string,
    ) {
    }
}

export class UserCreatedEventTest {
    constructor(
        public readonly userId: string,
    ) {
    }
}